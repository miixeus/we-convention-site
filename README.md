
  # We-web-site

  This is a code bundle for We-web-site. The original project is available at https://www.figma.com/design/hES9oD75nWnRzNvEsI9dNm/We-web-site.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  